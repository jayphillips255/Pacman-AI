#pragma once

// Game width and height are measured in number of tiles, not number of pixels.
constexpr int FPS = 60, GAME_WIDTH = 28, GAME_HEIGHT = 31,
    MAX_ENTITIES = 1000, MAX_ITEMS = 800, MAX_AGENTS = 5, MAX_GHOSTS = 4;

constexpr float TILE_WIDTH = 24;
